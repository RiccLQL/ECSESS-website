export enum InputTypes{
    text = 'text',
    dateTime = 'datetime',
}

export enum ButtonSizes{
  small = 'small',
  medium = 'medium',
}

export enum apiURLs{
  eventsBase = "/events",
  resourcesBase = "/resources",
  newsBase = "/news",
  livewireBase = "/livewire",
  photosBase = "/photos",
  jobsBase = "/jobs",
  councilBase = "/council",
}